language: node_js
node_js:
  - "10"
dist: bionic
sudo: required

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - dpkg
      - google-chrome-stable

script:
  - commitlint-travis
  - npm run lint
  - npm run test:ci
  - npm run build
